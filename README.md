# Snips-Bluetooth

```
sudo apt install snips-injection pulseaudio-module-bluetooth
sudo adduser _snips-skills bluetooth
sudo adduser pi bluetooth
sudo adduser pulse lp
pactl load-module module-bluetooth-discover
```
